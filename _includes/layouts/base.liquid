<!doctype html>
<html lang="{{ metadata.language }}">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>{{ title | default: metadata.title }}</title>
		<meta name="description" content="{{ description | default: metadata.description }}">

		{%- css "base" %}
		{% # Grouping base styles allows control of the order in which they are output %}
		{% include "public/css/fonts.css" %}
		{% include "public/css/colors.css" %}
		{% include "public/css/index.css" %}
		{% endcss %}

		{% # Render the CSS bundle using inlined CSS (for the fastest site performance in production)%}
		<style>{% getBundle "css" "base" %}</style>
		<style>{% getBundle "css" %}</style>

		{% # Renders the CSS bundle using a separate file, if you can't set CSP directive style-src: 'unsafe-inline' %}
		{% comment %}
		<link rel="stylesheet" href="{% getBundleFileUrl "css" %}">
		{% endcomment %}

		<!-- Google tag (gtag.js) -->
		<script async src="https://www.googletagmanager.com/gtag/js?id=G-94TD5DYSP0"></script>
		<script>
			window.dataLayer = window.dataLayer || [];
			function gtag(){dataLayer.push(arguments);}
			gtag('js', new Date());

			gtag('config', 'G-94TD5DYSP0');
		</script>
	</head>
	<body class="bg-ground text-body">
		<a href="#skip" class="sr-only">Skip to main content</a>

		{%- assign navPages = collections.all | eleventyNavigation %}
		{% render 'header' with navPages as navPages %}
		{%- js %}
			{% include "header-menu.js" %}
		{% endjs %}

		<main id="skip">
			{{ content }}
		</main>

		{% render 'footer' with navPages as navPages %}


		<!-- This page `{{ page.url }}` was built on {% currentBuildDate %} -->
		<script type="module" src="{% getBundleFileUrl "js" %}"></script>
	</body>
</html>
