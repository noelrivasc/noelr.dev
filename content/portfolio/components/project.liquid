		<div class="first:my-0 my-16 md:my-24 grid md:grid-cols-5 gap-8 md:gap-12">
			<div class="md:col-span-2">
				<h3 class="md:hidden text-2xl md:text-3xl font-serif font-bold text-body">
					{{ title }}
				</h3>
				{% if client %}
					<p class="md:hidden mb-8 text-lg md:text-xl font-serif font-regular text-body-subtle">{{ client }}</p>
				{% endif %}

				{% render '../components/carousel', images: images %}
			</div>
			<div class="md:col-span-3 space-y-4">
				<h3 class="hidden md:block text-2xl md:text-3xl font-serif font-bold text-body-subtle">
					{{ title }}
				</h3>
				{% if client %}
					<p class="hidden md:block text-lg md:text-xl font-serif font-regular text-body-subtle">{{ client }}</p>
				{% endif %}

				{{ body }}
			</div>
		</div>
